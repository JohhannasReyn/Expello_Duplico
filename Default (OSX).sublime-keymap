[
    // To use the suggested Key Bindings for Expelloduplico...
    /* ...just add a '/' to the beginning of this line (e.g. change this ' /* ' into this ' //* ')
    // "Expelloduplico" will Select (default) or Remove (optional) all duplicate entries found
    // in the document or in the users selected text.
    { "keys": ["super+shift+u"], "command": "expelloduplico"}
    //*/
]